<table class="table table-bordered table-hover col-sm-12 mt-5">
     <thead>
        <tr>
        <th scope="col">Name</th>
        <th scope="col">Last Name</th>
        <th scope="col">email</th>
        <th scope="col">Followed Stocks</th>
        <th scope="col">Actions</th>
        </tr>
    </thead>
    <tbody>
        <% current_user.friends.each do |friend| %>
            <tr>
            <th scope="row"><%= friend.first_name %></th>
            <td><%= friend.last_name %> </td>
            <td><%= friend.email %> </td>
            <td><%= friend.stocks.count %></td>
            <td><%= link_to 'Stop following', '#', method: :delete, data: {confirm: "Are you sure"}, class: "btn btn-sm btn-warning col-sm" %></td>
            </tr>
        <% end %>
    </tbody>
</table>